name: merge-check
on:
  pull_request:
    branches:
    - '**'
    branches-ignore:
    - main
  workflow_dispatch:
jobs:
  ubuntu-latest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: 'Execute Targets: Validate'
      run: dotnet run --verbosity q --project ./src/build/build.csproj -- Validate --plan
  windows-latest:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: 'Execute Targets: Validate'
      run: dotnet run --verbosity q --project ./src/build/build.csproj -- Validate --plan
  macos-latest:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: 'Execute Targets: Validate'
      run: dotnet run --verbosity q --project ./src/build/build.csproj -- Validate --plan