name: workflow
on:
  workflow_dispatch:
jobs:
  ubuntu-latest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: 'Execute Targets: Target'
      run: dotnet run --verbosity q --project ./src/build/build.csproj -- Target